<!DOCTYPE html>
<html>
  <head>
    <title>Mirabloques</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag "/favicon.ico" %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="stylesheet" media="screen" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap">
    <%= stylesheet_pack_tag 'application', media: 'all' %>
  </head>

  <body>
    <%= data_to_react("root", { colors: Color.all, categories: Category.popular }) %>
    <%= yield %>
  </body>
</html>
